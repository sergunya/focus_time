package com.focustime.nopplugin

/**
 * A minimal no-operation plugin for GoLand that demonstrates proper plugin structure
 * and follows JetBrains development guidelines.
 * 
 * This class exists to provide a proper Kotlin source structure, but performs no operations.
 * The plugin's functionality (or lack thereof) is defined in the plugin.xml file.
 */
class NoOpPlugin {
    // This plugin intentionally does nothing - no methods, no actions, no extensions
}
